function imageBorder(img) {
    img.style.border = "3px solid black";
    img.style.borderRadius = "3px";
}

function imageNormal(img) {
    img.style.border = null;
    img.style.borderRadius = null;
}